Algoritmo "entrada de dados"

Var
p1, p2: caracter
preco1, preco2, valor: real


Inicio
escreval("Quais produto você quer comprar? ")
leia(p1)
leia(p2)
escreval("Qual o preço do primeiro produto? ")
leia(preco1)
escreval("E do segundo? ")
leia(preco2)

valor <- preco1 + preco2

escreval("O valor a pagar é: ", valor:8:2)



Fimalgoritmo